server:
  port: 8004 #服务端口

spring:
  application:
    name: cloud-gateway-service #服务名
  cloud:
    nacos:
      discovery:
        server-addr: 172.18.0.2:30000 #配置Nacos地址
devtools:
  restart:
    enabled: true #是否支持热部署

